api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    collection:
        pagination:
            enabled: false
            items_per_page: 5  
            client_enabled: true
            client_items_per_page: true
    patch_formats:
        json: ['application/merge-patch+json']

    # swagger:
    #     versions: [3]

